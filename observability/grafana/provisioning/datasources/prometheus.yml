apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus
    access: proxy
    # Grafana reaches Prometheus by its compose service name on the docker network.
    url: http://prometheus:9090
    isDefault: true
    jsonData:
      timeInterval: 5s